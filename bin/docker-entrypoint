#!/bin/bash
set -e

# Apply database schema with ridgepole
echo "Applying database schema with ridgepole..."
bundle exec ridgepole -c config/database.yml -E production -f db/Schemafile -a

# Execute the main command
exec "$@"
