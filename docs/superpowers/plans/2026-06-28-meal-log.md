# 今日何食べた？ 食事記録機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 家族で「いつ・誰が・何を食べたか」を日毎に記録・共有し、食べ物をマスタ管理する機能を追加する。

**Architecture:** `Meal`（食事＝いつ・誰が）⇄ `MealFood`（中間）⇄ `Food`（食べ物マスタ）の多対多。記録UIは既存のカレンダー日次ビューに「今日何食べた？」セクションとして統合（日付ヘッダー直下）。食べ物マスタの管理は設定画面に新設する。`tasks` の side-panel / turbo_stream / `CalendarData` パターンを踏襲する。

**Tech Stack:** Rails 8, Ridgepole（`db/Schemafile`）, Slim, Turbo/Stimulus(TypeScript), Tailwind/daisyUI, RSpec + FactoryBot, vitest, SQLite。

## Global Constraints

- DB スキーマは Ridgepole 管理。マイグレーションは `db/Schemafile` を編集し `docker compose exec app bundle exec rails ridgepole:apply` で適用する。
- 全コマンドはコンテナ経由：`docker compose exec app <cmd>`（例 `docker compose exec app bundle exec rspec ...`）。
- commit 前に必ず `docker compose exec app bundle exec rubocop -A` と関連 spec を実行する。
- annotate gem は無効。モデル/ファクトリ/スペックの `# == Schema Information` ヘッダは**手書き**で既存慣習に合わせる。
- モデル名/テーブル名は英語、既存の単数形ノウン慣習（`task`, `plan`, `regular_task`）に揃える。
- 食べ物マスタは**削除しない**。整理は `active` フラグ（boolean, default true）で非表示にする。
- `meal_type` は整数（0:朝 / 1:昼 / 2:夕 / 3:間食）。Rails enum ではなく定数マップ方式（既存 `task.priority` 系の流儀）。
- 「誰が」は食事単位・任意（`user_id` null可、null=家族みんな）。
- 食べ物の「よく食べるもの」サジェストは `meal_foods` 件数で導出（専用カウントテーブルは作らない）。
- 仕様（参照元）: `docs/superpowers/specs/2026-06-28-meal-log-model-design.md`

## File Structure

**Models（新規）**
- `app/models/food.rb` — 食べ物マスタ（family スコープ、active フラグ、頻度スコープ）
- `app/models/meal.rb` — 食事記録（date / meal_type / user 任意、スコープ、meal_type ラベル）
- `app/models/meal_food.rb` — 食事↔食べ物の中間

**Models（変更）**
- `app/models/family.rb` — `has_many :foods, :meals`
- `app/models/user.rb` — `has_many :meals, dependent: :nullify`

**Schema**
- `db/Schemafile` — `foods` / `meals` / `meal_foods` 3テーブル追記

**Controllers（新規）**
- `app/controllers/foods_controller.rb` — マスタ管理（index/create/update、削除なし）
- `app/controllers/meals_controller.rb` — 記録 CRUD（new/create/edit/update/destroy、turbo_stream）

**Controllers（変更）**
- `app/controllers/concerns/calendar_data.rb` — `@family_meals` を両スコープで設定

**Views（新規）**
- `app/views/foods/index.html.slim` — マスタ管理画面（一覧＋追加＋リネーム＋表示/非表示）
- `app/views/meals/_form.html.slim` — side-panel 登録/編集フォーム
- `app/views/meals/new.html.slim` / `app/views/meals/edit.html.slim` — HTML フォールバック
- `app/views/calendar/_meal_section.html.slim` — 日次ビューの食事セクション
- `app/views/calendar/_meal_item.html.slim` — 食事1件の表示

**Views（変更）**
- `app/views/calendar/_daily_view.html.slim` — `family_meals` を渡す
- `app/views/calendar/_family_daily_view.html.slim` — ヘッダー直下に meal_section
- `app/views/calendar/_my_daily_view.html.slim` — ヘッダー直下に meal_section
- `app/views/settings/show.html.slim` — 食べ物マスタ管理へのリンク追加

**Helpers（変更）**
- `app/helpers/application_helper.rb` — `food_selector(family)` を追加

**JS（新規）**
- `app/javascript/controllers/meal_food_controller.ts` — 食べ物チップの追加/削除
- `app/javascript/controllers/side_panel_opener_controller.ts`（変更）— `openMealForm` / `openMealEdit` 追加

**Routes（変更）**
- `config/routes.rb` — `resources :meals, only: %i[new create edit update destroy]` / `resources :foods, only: %i[index create update]`

**Specs（新規）**
- `spec/factories/foods.rb`, `spec/factories/meals.rb`, `spec/factories/meal_foods.rb`
- `spec/models/meals/food_spec.rb`, `spec/models/meals/meal_spec.rb`, `spec/models/meals/meal_food_spec.rb`
- `spec/requests/meals/foods_spec.rb`, `spec/requests/meals/meals_spec.rb`
- `spec/javascript/controllers/meal_food_controller.test.ts`

**Specs（変更）**
- `spec/requests/calendar/calendar_spec.rb` — `@family_meals` のスコープ別表示

---

### Task 1: Food モデル + foods テーブル

**Files:**
- Modify: `db/Schemafile`
- Create: `app/models/food.rb`
- Modify: `app/models/family.rb`
- Create: `spec/factories/foods.rb`
- Test: `spec/models/meals/food_spec.rb`

**Interfaces:**
- Produces: `Food`（`belongs_to :family`、scopes `for_family(id)` / `active` / `ordered_by_name` / `frequently_used(limit=5)`、validations name presence/255/uniqueness scope family）。`Family#foods`。

- [ ] **Step 1: Schemafile に foods を追記**

`db/Schemafile` の末尾に追記:

```ruby
create_table 'foods', force: :cascade do |t|
  t.bigint 'family_id', null: false
  t.string 'name', null: false, limit: 255
  t.boolean 'active', null: false, default: true
  t.timestamps

  t.index %w[family_id name], name: 'index_foods_on_family_id_and_name', unique: true
  t.index %w[family_id active], name: 'index_foods_on_family_id_and_active'
end
```

- [ ] **Step 2: スキーマ適用**

Run: `docker compose exec app bundle exec rails ridgepole:apply`
Expected: `foods` テーブルが test/development に作成される（差分が apply される）。

- [ ] **Step 3: 失敗するモデルスペックを書く**

Create `spec/models/meals/food_spec.rb`:

```ruby
# == Schema Information
#
# Table name: foods
#
#  id         :integer          not null, primary key
#  active     :boolean          default(TRUE), not null
#  name       :string(255)      not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#
require 'rails_helper'

RSpec.describe Food, type: :model do
  let(:family) { create(:family) }

  describe 'validations' do
    it 'is valid with valid attributes' do
      expect(build(:food, family: family)).to be_valid
    end

    it 'is invalid without a name' do
      food = build(:food, family: family, name: nil)
      expect(food).not_to be_valid
      expect(food.errors[:name]).to be_present
    end

    it 'is invalid with a duplicate name in the same family' do
      create(:food, family: family, name: 'カレー')
      expect(build(:food, family: family, name: 'カレー')).not_to be_valid
    end

    it 'allows the same name in different families' do
      create(:food, family: family, name: 'カレー')
      expect(build(:food, family: create(:family), name: 'カレー')).to be_valid
    end
  end

  describe '.active' do
    it 'returns only active foods' do
      active = create(:food, family: family, active: true)
      create(:food, family: family, active: false)
      expect(family.foods.active).to eq([active])
    end
  end

  describe '.frequently_used' do
    it 'excludes inactive foods and orders by name when usage is equal, respecting limit' do
      a = create(:food, family: family, name: 'A', active: true)
      b = create(:food, family: family, name: 'B', active: true)
      create(:food, family: family, name: 'C', active: false) # hidden は出ない

      expect(family.foods.frequently_used(2)).to eq([a, b])
    end
  end
end
```

Note: 使用回数（meal_foods 件数）による並び順のテストは Task 3（meal_food factory 完成後）で追加する。

- [ ] **Step 4: スペックが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/models/meals/food_spec.rb`
Expected: FAIL（`uninitialized constant Food` / factory 未定義）

- [ ] **Step 5: Food モデルを実装**

Create `app/models/food.rb`:

```ruby
# == Schema Information
#
# Table name: foods
#
#  id         :integer          not null, primary key
#  active     :boolean          default(TRUE), not null
#  name       :string(255)      not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#
class Food < ApplicationRecord
  belongs_to :family
  has_many :meal_foods, dependent: :destroy
  has_many :meals, through: :meal_foods

  validates :name, presence: true, length: { maximum: 255 },
                   uniqueness: { scope: :family_id }

  scope :for_family, ->(family_id) { where(family_id: family_id) }
  scope :active, -> { where(active: true) }
  scope :ordered_by_name, -> { order(:name) }

  # よく食べるもの（meal_foods の件数で導出）。active のみ対象。
  scope :frequently_used, lambda { |limit = 5|
    active
      .left_joins(:meal_foods)
      .group(:id)
      .order(Arel.sql('COUNT(meal_foods.id) DESC'))
      .order(:name)
      .limit(limit)
  }
end
```

- [ ] **Step 6: Family に関連を追加**

In `app/models/family.rb`, `has_many :everyday_task_templates` の下に追記:

```ruby
  has_many :foods, dependent: :destroy
  has_many :meals, dependent: :destroy
```

- [ ] **Step 7: foods ファクトリを作成**

Create `spec/factories/foods.rb`:

```ruby
# == Schema Information
#
# Table name: foods
#
#  id         :integer          not null, primary key
#  active     :boolean          default(TRUE), not null
#  name       :string(255)      not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#
FactoryBot.define do
  factory :food do
    association :family
    sequence(:name) { |n| "食べ物#{n}" }
    active { true }
  end
end
```

- [ ] **Step 8: モデルスペックが green になることを確認**

Run: `docker compose exec app bundle exec rspec spec/models/meals/food_spec.rb`
Expected: PASS（Task 1 単体で自己完結。`frequently_used` は使用回数なしで active 絞り込み＋name 順＋limit を検証）

- [ ] **Step 9: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/models/food.rb app/models/family.rb spec/factories/foods.rb spec/models/meals/food_spec.rb db/Schemafile
git add db/Schemafile app/models/food.rb app/models/family.rb spec/factories/foods.rb spec/models/meals/food_spec.rb
git commit -m "feat: add Food master model and foods table"
```

---

### Task 2: Meal モデル + meals テーブル

**Files:**
- Modify: `db/Schemafile`
- Create: `app/models/meal.rb`
- Modify: `app/models/user.rb`
- Create: `spec/factories/meals.rb`
- Test: `spec/models/meals/meal_spec.rb`

**Interfaces:**
- Consumes: `Family#meals`（Task 1）。
- Produces: `Meal`（`belongs_to :family` / `belongs_to :user, optional: true`、scopes `for_date` / `for_family` / `for_user` / `visible_to_user(user_id)` / `ordered_by_meal_type`、`Meal::MEAL_TYPE_LABELS`、`Meal.meal_type_options`、`#meal_type_text`、validations date presence / meal_type presence+inclusion 0..3）。`User#meals`。

- [ ] **Step 1: Schemafile に meals を追記**

`db/Schemafile` の `foods` ブロックの下に追記:

```ruby
create_table 'meals', force: :cascade do |t|
  t.bigint 'family_id', null: false
  t.bigint 'user_id'
  t.date 'date', null: false
  t.integer 'meal_type', null: false # 0:朝 1:昼 2:夕 3:間食
  t.timestamps

  t.index %w[family_id date], name: 'index_meals_on_family_id_and_date'
  t.index %w[user_id date], name: 'index_meals_on_user_id_and_date'
  t.index %w[date meal_type], name: 'index_meals_on_date_and_meal_type'
end
```

- [ ] **Step 2: スキーマ適用**

Run: `docker compose exec app bundle exec rails ridgepole:apply`
Expected: `meals` テーブル作成。

- [ ] **Step 3: 失敗するモデルスペックを書く**

Create `spec/models/meals/meal_spec.rb`:

```ruby
# == Schema Information
#
# Table name: meals
#
#  id         :integer          not null, primary key
#  date       :date             not null
#  meal_type  :integer          not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#  user_id    :bigint
#
require 'rails_helper'

RSpec.describe Meal, type: :model do
  let(:family) { create(:family) }

  describe 'validations' do
    it 'is valid with valid attributes' do
      expect(build(:meal, family: family)).to be_valid
    end

    it 'is valid without a user (家族みんな)' do
      expect(build(:meal, family: family, user: nil)).to be_valid
    end

    it 'is invalid without a date' do
      expect(build(:meal, family: family, date: nil)).not_to be_valid
    end

    it 'is invalid with an out-of-range meal_type' do
      expect(build(:meal, family: family, meal_type: 4)).not_to be_valid
    end
  end

  describe '.visible_to_user' do
    it 'returns meals for the user or with no user' do
      me = create(:user, family: family)
      other = create(:user, family: family)
      mine = create(:meal, family: family, user: me)
      shared = create(:meal, family: family, user: nil)
      create(:meal, family: family, user: other)

      expect(family.meals.visible_to_user(me.id)).to contain_exactly(mine, shared)
    end
  end

  describe '#meal_type_text' do
    it 'returns the Japanese label' do
      expect(build(:meal, meal_type: 0).meal_type_text).to eq('朝')
      expect(build(:meal, meal_type: 3).meal_type_text).to eq('間食')
    end
  end

  describe '.meal_type_options' do
    it 'returns label/value pairs for selects' do
      expect(described_class.meal_type_options).to eq([['朝', 0], ['昼', 1], ['夕', 2], ['間食', 3]])
    end
  end
end
```

- [ ] **Step 4: スペックが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/models/meals/meal_spec.rb`
Expected: FAIL（`Meal` / factory 未定義）

- [ ] **Step 5: Meal モデルを実装**

Create `app/models/meal.rb`:

```ruby
# == Schema Information
#
# Table name: meals
#
#  id         :integer          not null, primary key
#  date       :date             not null
#  meal_type  :integer          not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#  user_id    :bigint
#
class Meal < ApplicationRecord
  MEAL_TYPE_LABELS = { 0 => '朝', 1 => '昼', 2 => '夕', 3 => '間食' }.freeze

  belongs_to :family
  belongs_to :user, optional: true
  has_many :meal_foods, dependent: :destroy
  has_many :foods, through: :meal_foods

  validates :date, presence: true
  validates :meal_type, presence: true, inclusion: { in: MEAL_TYPE_LABELS.keys }

  scope :for_date, ->(date) { where(date: date) }
  scope :for_family, ->(family_id) { where(family_id: family_id) }
  scope :for_user, ->(user_id) { where(user_id: user_id) }
  scope :visible_to_user, ->(user_id) { where(user_id: [user_id, nil]) }
  scope :ordered_by_meal_type, -> { order(:meal_type, :created_at) }

  def self.meal_type_options
    MEAL_TYPE_LABELS.map { |value, label| [label, value] }
  end

  def meal_type_text
    MEAL_TYPE_LABELS.fetch(meal_type, '未設定')
  end
end
```

- [ ] **Step 6: User に関連を追加**

In `app/models/user.rb`, `has_many :plan_participants` の下に追記:

```ruby
  has_many :meals, dependent: :nullify
```

- [ ] **Step 7: meals ファクトリを作成**

Create `spec/factories/meals.rb`:

```ruby
# == Schema Information
#
# Table name: meals
#
#  id         :integer          not null, primary key
#  date       :date             not null
#  meal_type  :integer          not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  family_id  :bigint           not null
#  user_id    :bigint
#
FactoryBot.define do
  factory :meal do
    association :family
    user { nil }
    date { Time.zone.today }
    meal_type { 1 } # 昼
  end
end
```

- [ ] **Step 8: モデルスペックを実行（Task 3 の meal_food factory がまだ無いので meal_spec のみ green を確認）**

Run: `docker compose exec app bundle exec rspec spec/models/meals/meal_spec.rb`
Expected: PASS

- [ ] **Step 9: コミット**

```bash
docker compose exec app bundle exec rubocop -A app/models/meal.rb app/models/user.rb spec/factories/meals.rb spec/models/meals/meal_spec.rb db/Schemafile
git add db/Schemafile app/models/meal.rb app/models/user.rb spec/factories/meals.rb spec/models/meals/meal_spec.rb
git commit -m "feat: add Meal model and meals table"
```

---

### Task 3: MealFood 中間モデル + meal_foods テーブル

**Files:**
- Modify: `db/Schemafile`
- Create: `app/models/meal_food.rb`
- Create: `spec/factories/meal_foods.rb`
- Test: `spec/models/meals/meal_food_spec.rb`

**Interfaces:**
- Consumes: `Meal#meal_foods`（Task 2）、`Food#meal_foods`（Task 1）。
- Produces: `MealFood`（`belongs_to :meal` / `belongs_to :food`、validation food_id uniqueness scope meal_id）。

- [ ] **Step 1: Schemafile に meal_foods を追記**

`db/Schemafile` の `meals` ブロックの下に追記:

```ruby
create_table 'meal_foods', force: :cascade do |t|
  t.bigint 'meal_id', null: false
  t.bigint 'food_id', null: false
  t.timestamps

  t.index %w[meal_id food_id], name: 'index_meal_foods_on_meal_id_and_food_id', unique: true
  t.index ['food_id'], name: 'index_meal_foods_on_food_id'
end
```

- [ ] **Step 2: スキーマ適用**

Run: `docker compose exec app bundle exec rails ridgepole:apply`
Expected: `meal_foods` テーブル作成。

- [ ] **Step 3: 失敗するモデルスペックを書く**

Create `spec/models/meals/meal_food_spec.rb`:

```ruby
# == Schema Information
#
# Table name: meal_foods
#
#  id         :integer          not null, primary key
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  food_id    :bigint           not null
#  meal_id    :bigint           not null
#
require 'rails_helper'

RSpec.describe MealFood, type: :model do
  let(:family) { create(:family) }
  let(:meal) { create(:meal, family: family) }
  let(:food) { create(:food, family: family) }

  it 'is valid with a meal and food' do
    expect(build(:meal_food, meal: meal, food: food)).to be_valid
  end

  it 'does not allow the same food twice in one meal' do
    create(:meal_food, meal: meal, food: food)
    expect(build(:meal_food, meal: meal, food: food)).not_to be_valid
  end

  it 'is destroyed when its meal is destroyed' do
    create(:meal_food, meal: meal, food: food)
    expect { meal.destroy }.to change(MealFood, :count).by(-1)
  end
end
```

- [ ] **Step 4: スペックが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/models/meals/meal_food_spec.rb`
Expected: FAIL（`MealFood` / factory 未定義）

- [ ] **Step 5: MealFood モデルを実装**

Create `app/models/meal_food.rb`:

```ruby
# == Schema Information
#
# Table name: meal_foods
#
#  id         :integer          not null, primary key
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  food_id    :bigint           not null
#  meal_id    :bigint           not null
#
class MealFood < ApplicationRecord
  belongs_to :meal
  belongs_to :food

  validates :food_id, uniqueness: { scope: :meal_id }
end
```

- [ ] **Step 6: meal_foods ファクトリを作成**

Create `spec/factories/meal_foods.rb`:

```ruby
# == Schema Information
#
# Table name: meal_foods
#
#  id         :integer          not null, primary key
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  food_id    :bigint           not null
#  meal_id    :bigint           not null
#
FactoryBot.define do
  factory :meal_food do
    association :meal
    association :food
  end
end
```

- [ ] **Step 7: Food.frequently_used の使用回数順テストを追加（meal_food factory が揃ったため）**

`spec/models/meals/food_spec.rb` の `.frequently_used` describe に追記:

```ruby
    it 'orders by usage count desc (derived from meal_foods)' do
      a = create(:food, family: family, name: 'A')
      b = create(:food, family: family, name: 'B')
      create(:meal_food, meal: create(:meal, family: family), food: b)
      create(:meal_food, meal: create(:meal, family: family), food: b)
      create(:meal_food, meal: create(:meal, family: family), food: a)

      expect(family.foods.frequently_used(2).map(&:name)).to eq(%w[B A])
    end
```

- [ ] **Step 8: Task 1〜3 のモデルスペックをまとめて実行**

Run: `docker compose exec app bundle exec rspec spec/models/meals`
Expected: PASS（food_spec の使用回数順テスト含め全 green）

- [ ] **Step 9: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/models/meal_food.rb spec/factories/meal_foods.rb spec/models/meals/meal_food_spec.rb db/Schemafile
git add db/Schemafile app/models/meal_food.rb spec/factories/meal_foods.rb spec/models/meals/meal_food_spec.rb
git commit -m "feat: add MealFood join model and meal_foods table"
```

---

### Task 4: CalendarData に @family_meals を追加

**Files:**
- Modify: `app/controllers/concerns/calendar_data.rb`

**Interfaces:**
- Consumes: `Meal.visible_to_user`、`Family#meals`（Task 2）。
- Produces: `@family_meals`（family スコープ＝その日の全 family 食事 / my スコープ＝`user_id` が自分 or nil）。daily_view 系の描画で利用。

**Note:** これはインフラ的変更（ivar 追加）で、まだビューで参照されない（Task 10 で参照）。表示の振る舞いテストは Task 10 で追加する。本タスクは既存 calendar spec が green のまま（=新クエリが描画を壊さない）であることで検証する。

- [ ] **Step 1: CalendarData に @family_meals を設定**

In `app/controllers/concerns/calendar_data.rb`:

`set_my_calendar_data` の末尾（`@family_tasks = ...` の下）に追記:

```ruby
    @family_meals = current_user.family.meals.for_date(date)
                                .visible_to_user(current_user.id)
                                .includes(:user, :foods)
                                .ordered_by_meal_type
```

`set_family_calendar_data` の末尾（`@family_tasks = ...` の下）に追記:

```ruby
    @family_meals = current_user.family.meals.for_date(date)
                                .includes(:user, :foods)
                                .ordered_by_meal_type
```

- [ ] **Step 2: 既存の calendar spec が green のままであることを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/calendar/calendar_spec.rb`
Expected: PASS（新クエリが既存の描画/応答を壊していないこと）

- [ ] **Step 3: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/controllers/concerns/calendar_data.rb
git add app/controllers/concerns/calendar_data.rb
git commit -m "feat: load @family_meals in CalendarData for both scopes"
```

---

### Task 5: FoodsController（マスタ管理 index/create/update）

**Files:**
- Modify: `config/routes.rb`
- Create: `app/controllers/foods_controller.rb`
- Test: `spec/requests/meals/foods_spec.rb`

**Interfaces:**
- Consumes: `Family#foods`, `Food.ordered_by_name`（Task 1）。
- Produces: ルート `foods_path`(GET/POST), `food_path(food)`(PATCH)。`@foods`（管理画面用）。`destroy` は存在しない。

- [ ] **Step 1: ルートを追加**

In `config/routes.rb`, `resources :regular_tasks, only: %i[index create]` の下に追記:

```ruby
  resources :foods, only: %i[index create update]
  resources :meals, only: %i[new create edit update destroy]
```

（`meals` ルートは Task 7 で使用。ここでまとめて追加しておく。）

- [ ] **Step 2: 失敗するリクエストスペックを書く**

Create `spec/requests/meals/foods_spec.rb`:

```ruby
require 'rails_helper'

RSpec.describe 'Foods', type: :request do
  let(:family) { create(:family) }
  let(:user) { create(:user, family: family) }

  before { sign_in user, scope: :user }

  describe 'GET /foods' do
    it 'lists family foods including inactive ones' do
      create(:food, family: family, name: 'ラーメン', active: true)
      create(:food, family: family, name: 'カレー', active: false)
      get foods_path
      expect(response).to have_http_status(:ok)
      expect(response.body).to include('ラーメン')
      expect(response.body).to include('カレー')
    end
  end

  describe 'POST /foods' do
    it 'creates a food' do
      expect do
        post foods_path, params: { food: { name: '新しい食べ物' } }
      end.to change(Food, :count).by(1)
      expect(response).to redirect_to(foods_path)
    end

    it 'rejects a duplicate name' do
      create(:food, family: family, name: '重複')
      expect do
        post foods_path, params: { food: { name: '重複' } }
      end.not_to change(Food, :count)
      expect(response).to have_http_status(:unprocessable_entity)
    end
  end

  describe 'PATCH /foods/:id' do
    let(:food) { create(:food, family: family, name: '旧名', active: true) }

    it 'renames the food' do
      patch food_path(food), params: { food: { name: '新名' } }
      expect(food.reload.name).to eq('新名')
    end

    it 'toggles active to false' do
      patch food_path(food), params: { food: { active: '0' } }
      expect(food.reload.active).to be(false)
    end

    it 'does not update a food from another family' do
      other = create(:food, name: '他家族')
      expect { patch food_path(other), params: { food: { name: 'x' } } }
        .to raise_error(ActiveRecord::RecordNotFound)
    end
  end
end
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/foods_spec.rb`
Expected: FAIL（`FoodsController` 未定義、view 未作成）

- [ ] **Step 4: FoodsController を実装**

Create `app/controllers/foods_controller.rb`:

```ruby
class FoodsController < ApplicationController
  before_action :authenticate_user!
  before_action :set_food, only: %i[update]

  def index
    @food = current_user.family.foods.build
    @foods = current_user.family.foods.ordered_by_name
  end

  def create
    @food = current_user.family.foods.build(food_params)
    if @food.save
      redirect_to foods_path, notice: '食べ物を追加しました'
    else
      @foods = current_user.family.foods.ordered_by_name
      render :index, status: :unprocessable_entity
    end
  end

  def update
    if @food.update(food_params)
      redirect_to foods_path, notice: '食べ物を更新しました'
    else
      @foods = current_user.family.foods.ordered_by_name
      render :index, status: :unprocessable_entity
    end
  end

  private

  def set_food
    @food = current_user.family.foods.find(params[:id])
  end

  def food_params
    params.expect(food: %i[name active])
  end
end
```

- [ ] **Step 5: 管理画面ビューを作成**（Task 6 で本格化するが、テストを通す最小実装）

Create `app/views/foods/index.html.slim`:

```slim
.container.mx-auto.p-4.max-w-md
  .flex.items-center.justify-between.mb-6
    h1.text-2xl.font-bold 食べ物マスタ
    = link_to settings_path, class: 'btn btn-ghost btn-sm' do
      i.fas.fa-arrow-left.mr-1
      | 設定へ

  .card.bg-base-100.shadow-xl.mb-4
    .card-body
      = form_with model: @food, url: foods_path, method: :post do |f|
        - if @food.errors.any?
          .alert.alert-error.mb-2
            ul
              - @food.errors.full_messages.each do |msg|
                li = msg
        .flex.items-center.gap-2
          = f.text_field :name, class: 'input input-bordered flex-1', placeholder: '食べ物の名前'
          = f.submit '追加', class: 'btn btn-primary'

  .card.bg-base-100.shadow-xl
    .card-body.p-0
      - if @foods.any?
        ul.divide-y
          - @foods.each do |food|
            li.p-3 = render 'foods/food_row', food: food
      - else
        .p-4.text-gray-400.text-sm まだ登録がありません
```

Create `app/views/foods/_food_row.html.slim`:

```slim
= form_with model: food, url: food_path(food), method: :patch, class: 'flex items-center gap-2' do |f|
  = f.text_field :name, class: "input input-bordered input-sm flex-1 #{'opacity-50' unless food.active}"
  label.cursor-pointer.label.gap-1
    span.label-text.text-xs= food.active ? '表示' : '非表示'
    = f.check_box :active, class: 'toggle toggle-sm toggle-primary'
  = f.submit '保存', class: 'btn btn-sm btn-outline'
```

Note: `check_box` は未チェック時に `active=0` を hidden で送るため、トグルOFF→false が正しく届く。

- [ ] **Step 6: テストが通ることを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/foods_spec.rb`
Expected: PASS

- [ ] **Step 7: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/controllers/foods_controller.rb config/routes.rb
git add config/routes.rb app/controllers/foods_controller.rb app/views/foods spec/requests/meals/foods_spec.rb
git commit -m "feat: add FoodsController and food master management screen"
```

---

### Task 6: 設定画面に食べ物マスタ管理リンクを追加

**Files:**
- Modify: `app/views/settings/show.html.slim`
- Test: `spec/requests/settings/settings_spec.rb`

**Interfaces:**
- Consumes: `foods_path`（Task 5）。

- [ ] **Step 1: settings_spec にリンク存在のテストを追加**

`spec/requests/settings/settings_spec.rb` の `GET /settings` 系 describe に追加:

```ruby
    it 'shows a link to the food master management' do
      get settings_path
      expect(response.body).to include(foods_path)
      expect(response.body).to include('食べ物マスタ')
    end
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/settings/settings_spec.rb -e "food master"`
Expected: FAIL

- [ ] **Step 3: 設定画面にリンクを追加**

In `app/views/settings/show.html.slim`, 「毎日のタスク管理」セクション（`= link_to everyday_task_templates_path ...` のブロック）の直後に、同形式で追記:

```slim
      .divider.my-2

      / 食べ物マスタの管理
      .form-control
        label.label
          span.label-text 食べ物マスタの管理
        = link_to foods_path, class: 'btn btn-outline btn-sm w-full justify-between' do
          span
            i.fas.fa-utensils.mr-2
            | 食べ物マスタを管理
          i.fas.fa-chevron-right
```

- [ ] **Step 4: テストが通ることを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/settings/settings_spec.rb`
Expected: PASS

- [ ] **Step 5: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/views/settings/show.html.slim || true
git add app/views/settings/show.html.slim spec/requests/settings/settings_spec.rb
git commit -m "feat: link food master management from settings"
```

---

### Task 7: MealsController（記録 CRUD・turbo_stream）

**Files:**
- Create: `app/controllers/meals_controller.rb`
- Create: `app/views/meals/new.html.slim`
- Create: `app/views/meals/edit.html.slim`
- Create: `app/views/meals/_form.html.slim`（最小版。Task 9 で本格化）
- Test: `spec/requests/meals/meals_spec.rb`

**Interfaces:**
- Consumes: ルート `meals`（Task 5 Step 1）、`CalendarData#set_calendar_data`（Task 4）、`Family#meals`/`Family#foods`（Task 1,2）。
- Produces: `create`/`update` は `meal[food_names][]` から `Food` を find_or_initialize して `meal_foods` を構築。turbo_stream で `daily_details` 更新＋`side-panel` クリア。食べ物0件は `:unprocessable_entity`。

- [ ] **Step 1: 失敗するリクエストスペックを書く**

Create `spec/requests/meals/meals_spec.rb`:

```ruby
require 'rails_helper'

RSpec.describe 'Meals', type: :request do
  let(:family) { create(:family) }
  let(:user) { create(:user, family: family) }

  before { sign_in user, scope: :user }

  describe 'POST /meals' do
    let(:params) do
      {
        meal: { date: Time.zone.today.to_s, meal_type: 1, user_id: '', food_names: ['ラーメン', '餃子'] },
        scope: 'family'
      }
    end

    it 'creates a meal with foods (find_or_create master)' do
      expect do
        post meals_path, params: params, as: :turbo_stream
      end.to change(Meal, :count).by(1).and change(Food, :count).by(2).and change(MealFood, :count).by(2)

      expect(response).to have_http_status(:ok)
      expect(response.body).to include('turbo-stream action="update" target="daily_details"')
      expect(Meal.last.user_id).to be_nil
    end

    it 'reuses an existing food master' do
      create(:food, family: family, name: 'ラーメン')
      expect do
        post meals_path, params: { meal: { date: Time.zone.today.to_s, meal_type: 1, food_names: ['ラーメン'] }, scope: 'family' }, as: :turbo_stream
      end.to change(Food, :count).by(0).and change(Meal, :count).by(1)
    end

    it 'rejects a meal with no foods' do
      expect do
        post meals_path, params: { meal: { date: Time.zone.today.to_s, meal_type: 1, food_names: [] }, scope: 'family' }, as: :turbo_stream
      end.not_to change(Meal, :count)
      expect(response).to have_http_status(:unprocessable_entity)
    end
  end

  describe 'PATCH /meals/:id' do
    let(:meal) { create(:meal, family: family, user: user, meal_type: 1) }

    before do
      create(:meal_food, meal: meal, food: create(:food, family: family, name: '古い'))
    end

    it 'replaces the foods' do
      patch meal_path(meal), params: { meal: { date: meal.date.to_s, meal_type: 2, food_names: ['新しい'] }, scope: 'family' }, as: :turbo_stream
      expect(response).to have_http_status(:ok)
      expect(meal.reload.foods.map(&:name)).to eq(['新しい'])
      expect(meal.meal_type).to eq(2)
    end

    it 'does not update a meal from another family' do
      other = create(:meal)
      expect { patch meal_path(other), params: { meal: { date: other.date.to_s, meal_type: 1, food_names: ['x'] } }, as: :turbo_stream }
        .to raise_error(ActiveRecord::RecordNotFound)
    end
  end

  describe 'DELETE /meals/:id' do
    it 'destroys the meal' do
      meal = create(:meal, family: family, user: user)
      create(:meal_food, meal: meal, food: create(:food, family: family))
      expect do
        delete meal_path(meal), as: :turbo_stream
      end.to change(Meal, :count).by(-1)
      expect(response).to have_http_status(:ok)
    end
  end
end
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/meals_spec.rb`
Expected: FAIL（`MealsController` 未定義）

- [ ] **Step 3: MealsController を実装**

Create `app/controllers/meals_controller.rb`:

```ruby
class MealsController < ApplicationController
  include CalendarData

  before_action :authenticate_user!
  before_action :set_meal, only: %i[edit update destroy]

  FOOD_REQUIRED_MESSAGE = '食べ物を1つ以上選択してください'

  def new
    @meal = current_user.family.meals.build(default_meal_attributes)
    render_form
  end

  def edit
    render_form
  end

  def create
    @meal = current_user.family.meals.build(meal_params)
    @meal.user_id = nil if @meal.user_id.blank?
    save_with_foods('食事を記録しました')
  end

  def update
    @meal.assign_attributes(meal_params)
    @meal.user_id = nil if @meal.user_id.blank?
    save_with_foods('食事を更新しました', reassign: true)
  end

  def destroy
    date = @meal.date
    @meal.destroy
    set_calendar_data(date)
    respond_to do |format|
      format.turbo_stream do
        render turbo_stream: turbo_stream.update('daily_details',
                                                 partial: 'calendar/daily_view', locals: { date: @date })
      end
      format.html { redirect_to calendar_path(date: date), notice: '食事を削除しました' }
    end
  end

  private

  def set_meal
    @meal = current_user.family.meals.find(params[:id])
  end

  def meal_params
    params.expect(meal: %i[date meal_type user_id])
  end

  def food_names
    Array(params.dig(:meal, :food_names)).map { |name| name.to_s.strip }.reject(&:blank?).uniq
  end

  def save_with_foods(notice, reassign: false)
    if food_names.empty?
      @meal.errors.add(:base, FOOD_REQUIRED_MESSAGE)
      return render_form_with_errors
    end

    saved = false
    Meal.transaction do
      @meal.meal_foods.destroy_all if reassign
      assign_foods(@meal)
      saved = @meal.save
      raise ActiveRecord::Rollback unless saved
    end

    saved ? render_saved(notice) : render_form_with_errors
  end

  def assign_foods(meal)
    food_names.each do |name|
      food = current_user.family.foods.where(name: name).first_or_initialize
      meal.meal_foods.build(food: food)
    end
  end

  def default_meal_attributes
    { date: parsed_date, user_id: my_scope? ? current_user.id : nil }
  end

  def parsed_date
    params[:date].present? ? Date.parse(params[:date]) : Time.zone.today
  rescue Date::Error
    Time.zone.today
  end

  def render_form
    respond_to do |format|
      format.turbo_stream do
        render turbo_stream: turbo_stream.update('side-panel', partial: 'meals/form',
                                                               locals: { meal: @meal, scope: current_scope })
      end
      format.html { render(@meal.persisted? ? :edit : :new) }
    end
  end

  def render_form_with_errors
    respond_to do |format|
      format.turbo_stream do
        render turbo_stream: turbo_stream.update('side-panel', partial: 'meals/form',
                                                               locals: { meal: @meal, scope: current_scope }),
               status: :unprocessable_entity
      end
      format.html { render(@meal.persisted? ? :edit : :new), status: :unprocessable_entity }
    end
  end

  def render_saved(notice)
    set_calendar_data(@meal.date)
    respond_to do |format|
      format.turbo_stream do
        render turbo_stream: [
          turbo_stream.update('daily_details', partial: 'calendar/daily_view', locals: { date: @date }),
          turbo_stream.update('side-panel', '')
        ]
      end
      format.html { redirect_to calendar_path(date: @meal.date), notice: notice }
    end
  end
end
```

- [ ] **Step 4: フォーム/HTML ビューの最小版を作成**

Create `app/views/meals/_form.html.slim`（Task 9 で本格化。まずテストを通す最小構成）:

```slim
.side-panel-overlay[
  data-controller="side-panel"
  data-side-panel-target="overlay"
  data-action="click->side-panel#closeBackground"
  style="display: block; position: fixed; top: 0; left: 0; right: 0; bottom: 0; z-index: 9999;"
]
  .side-panel-backdrop.opacity-0 data-side-panel-target="backdrop"
  .side-panel.translate-x-full data-side-panel-target="panel"
    .side-panel-content
      .side-panel-header
        h3.text-lg.font-semibold = meal.persisted? ? '食事を編集' : '食事を記録'
        button.btn.btn-sm.btn-circle.btn-ghost data-action="click->side-panel#close"
          i.fas.fa-times
      .side-panel-body
        = form_with model: meal do |f|
          = hidden_field_tag :scope, scope
          .space-y-6
            - if meal.errors.any?
              .alert.alert-error
                ul
                  - meal.errors.full_messages.each do |msg|
                    li = msg
            .form-control
              = f.label :meal_type, '食事区分', class: 'label'
              = f.select :meal_type, Meal.meal_type_options, {}, class: 'select select-bordered w-full'
            .form-control
              = f.label :date, '日付', class: 'label'
              = mobile_date_field f, :date
            .form-control
              span.label-text 食べ物
              - meal.foods.each do |food|
                input type="hidden" name="meal[food_names][]" value=food.name
              input type="text" name="meal[food_names][]" class="input input-bordered w-full" placeholder="食べ物（複数可）"
          .side-panel-footer
            = link_to 'キャンセル', '#', class: 'btn btn-ghost', data: { action: 'click->side-panel#close' }
            = f.submit meal.persisted? ? '更新' : '記録', class: 'btn btn-primary'
```

Create `app/views/meals/new.html.slim`:

```slim
#side-panel
  = render 'meals/form', meal: @meal, scope: current_scope
```

Create `app/views/meals/edit.html.slim`:

```slim
#side-panel
  = render 'meals/form', meal: @meal, scope: current_scope
```

- [ ] **Step 5: テストが通ることを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/meals_spec.rb`
Expected: PASS

- [ ] **Step 6: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/controllers/meals_controller.rb
git add app/controllers/meals_controller.rb app/views/meals spec/requests/meals/meals_spec.rb
git commit -m "feat: add MealsController with turbo_stream CRUD and food find_or_create"
```

---

### Task 8: meal-food Stimulus コントローラ（チップ） + side-panel-opener 拡張

**Files:**
- Create: `app/javascript/controllers/meal_food_controller.ts`
- Modify: `app/javascript/controllers/side_panel_opener_controller.ts`
- Modify: `app/javascript/controllers/index.js`（コントローラ登録。import + application.register 形式）
- Test: `spec/javascript/controllers/meal_food_controller.test.ts`

**Interfaces:**
- Produces: Stimulus `meal-food`（actions: `quickSelect`, `selectFromList`, `addFromInput`, `removeFood`, `openSelect`, `closeSelect`; targets: `chips`, `input`, `selectModal`）。各チップは hidden `meal[food_names][]` を保持。`side-panel-opener#openMealForm` / `#openMealEdit`。

- [ ] **Step 1: 失敗する JS テストを書く**

Create `spec/javascript/controllers/meal_food_controller.test.ts`:

```ts
import { Application } from "@hotwired/stimulus"
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest"

import MealFoodController from "../../../app/javascript/controllers/meal_food_controller"

const flush = () => new Promise(resolve => setTimeout(resolve, 0))

const html = `
  <div data-controller="meal-food">
    <button id="quick" type="button" data-action="meal-food#quickSelect" data-food-name="ラーメン"></button>
    <div data-meal-food-target="chips"></div>
    <input data-meal-food-target="input" />
    <button id="add" type="button" data-action="meal-food#addFromInput"></button>
  </div>
`

describe("meal-food controller", () => {
  let application: Application

  beforeEach(async () => {
    HTMLDialogElement.prototype.showModal = vi.fn()
    HTMLDialogElement.prototype.close = vi.fn()
    document.body.innerHTML = html
    application = Application.start()
    application.register("meal-food", MealFoodController)
    await flush()
  })

  afterEach(async () => {
    application.stop()
    document.body.innerHTML = ""
    await flush()
    vi.restoreAllMocks()
  })

  const names = () =>
    Array.from(document.querySelectorAll<HTMLInputElement>('input[name="meal[food_names][]"]')).map(i => i.value)

  it("adds a chip with a hidden input on quick select", async () => {
    document.querySelector<HTMLButtonElement>("#quick")!.click()
    await flush()
    expect(names()).toEqual(["ラーメン"])
  })

  it("adds from text input and clears it", async () => {
    const input = document.querySelector<HTMLInputElement>('[data-meal-food-target="input"]')!
    input.value = "カレー"
    document.querySelector<HTMLButtonElement>("#add")!.click()
    await flush()
    expect(names()).toEqual(["カレー"])
    expect(input.value).toBe("")
  })

  it("does not add duplicates", async () => {
    const btn = document.querySelector<HTMLButtonElement>("#quick")!
    btn.click()
    btn.click()
    await flush()
    expect(names()).toEqual(["ラーメン"])
  })

  it("removes a chip", async () => {
    document.querySelector<HTMLButtonElement>("#quick")!.click()
    await flush()
    document.querySelector<HTMLButtonElement>('[data-action="meal-food#removeFood"]')!.click()
    await flush()
    expect(names()).toEqual([])
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `yarn test:js spec/javascript/controllers/meal_food_controller.test.ts`
Expected: FAIL（controller 未実装）

- [ ] **Step 3: meal-food コントローラを実装**

Create `app/javascript/controllers/meal_food_controller.ts`:

```ts
import { Controller } from "@hotwired/stimulus"

/**
 * Meal Food Controller
 * 食べ物をチップとして追加/削除し、meal[food_names][] を送信する
 */
export default class extends Controller {
	static targets = ["chips", "input", "selectModal"]

	declare chipsTarget: HTMLElement
	declare inputTarget: HTMLInputElement
	declare selectModalTarget: HTMLDialogElement
	declare hasSelectModalTarget: boolean

	quickSelect(event: Event) {
		const name = (event.currentTarget as HTMLElement).dataset.foodName
		if (name) this.addFood(name)
	}

	selectFromList(event: Event) {
		const name = (event.currentTarget as HTMLElement).dataset.foodName
		if (name) this.addFood(name)
		this.closeSelect()
	}

	addFromInput() {
		const name = this.inputTarget.value.trim()
		if (name) {
			this.addFood(name)
			this.inputTarget.value = ""
		}
		this.inputTarget.focus()
	}

	removeFood(event: Event) {
		const btn = event.currentTarget as HTMLElement
		btn.closest("[data-food-name]")?.remove()
	}

	openSelect() {
		if (this.hasSelectModalTarget) this.selectModalTarget.showModal()
	}

	closeSelect() {
		if (this.hasSelectModalTarget) this.selectModalTarget.close()
	}

	private addFood(name: string) {
		if (this.existingNames().includes(name)) return

		const chip = document.createElement("span")
		chip.className = "badge badge-lg gap-1"
		chip.dataset.foodName = name

		const hidden = document.createElement("input")
		hidden.type = "hidden"
		hidden.name = "meal[food_names][]"
		hidden.value = name

		const label = document.createElement("span")
		label.textContent = name

		const remove = document.createElement("button")
		remove.type = "button"
		remove.textContent = "✕"
		remove.dataset.action = "meal-food#removeFood"

		chip.append(hidden, label, remove)
		this.chipsTarget.appendChild(chip)
	}

	private existingNames(): string[] {
		return Array.from(this.chipsTarget.querySelectorAll<HTMLElement>("[data-food-name]")).map(
			el => el.dataset.foodName || "",
		)
	}
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `yarn test:js spec/javascript/controllers/meal_food_controller.test.ts`
Expected: PASS

- [ ] **Step 5: side-panel-opener に meal メソッドを追加**

In `app/javascript/controllers/side_panel_opener_controller.ts`, `openTaskEdit()` の下に追記:

```ts
  openMealForm(): void {
    const date = (this.element as HTMLElement).dataset.date
    const scope = this.getScope()
    this.openSidePanel(`/meals/new?date=${date}&scope=${scope}`)
  }

  openMealEdit(): void {
    const mealId = (this.element as HTMLElement).dataset.mealId
    const scope = this.getScope()
    this.openSidePanel(`/meals/${mealId}/edit?scope=${scope}`)
  }
```

- [ ] **Step 6: コントローラを登録**

In `app/javascript/controllers/index.js`（既存の `import RegularTaskController ...` / `application.register("regular-task", ...)` のブロックに倣って）追記:

```js
import MealFoodController from "./meal_food_controller"
application.register("meal-food", MealFoodController)
```

（esbuild が `.ts` ソースを解決するため import 拡張子は不要。）

- [ ] **Step 7: JS ビルド確認 & コミット**

Run: `yarn build` （エラーが出ないこと）
Run: `yarn test:js`（全 JS テスト green）

```bash
git add app/javascript/controllers/meal_food_controller.ts app/javascript/controllers/side_panel_opener_controller.ts app/javascript/controllers/index.js spec/javascript/controllers/meal_food_controller.test.ts
git commit -m "feat: add meal-food stimulus controller and side-panel-opener meal actions"
```

---

### Task 9: meal フォーム本格版（food_selector ヘルパー + チップUI）

**Files:**
- Modify: `app/helpers/application_helper.rb`
- Modify: `app/views/meals/_form.html.slim`
- Test: `spec/requests/meals/meals_spec.rb`（フォーム描画の追加アサーション）

**Interfaces:**
- Consumes: `Family#foods`, `Food.frequently_used` / `Food.active`（Task 1）、`meal-food` controller（Task 8）、`Meal.meal_type_options`（Task 2）、`action_sheet_modal`（既存ヘルパー）。
- Produces: `food_selector(family)` ヘルパー（よく食べるもののクイックボタン＋全 active のモーダル）。

- [ ] **Step 1: フォーム描画テストを追加**

`spec/requests/meals/meals_spec.rb` に describe 追加:

```ruby
  describe 'GET /meals/new' do
    it 'renders the form with frequently used foods as quick buttons' do
      create(:food, family: family, name: 'ラーメン', active: true)
      get new_meal_path(date: Time.zone.today.to_s, scope: 'family'), as: :turbo_stream
      expect(response).to have_http_status(:ok)
      expect(response.body).to include('meal-food')
      expect(response.body).to include('ラーメン')
    end
  end

  describe 'GET /meals/:id/edit' do
    it 'pre-fills chips with existing foods' do
      meal = create(:meal, family: family, user: user, meal_type: 1)
      create(:meal_food, meal: meal, food: create(:food, family: family, name: 'カレー'))
      get edit_meal_path(meal, scope: 'family'), as: :turbo_stream
      expect(response.body).to include('カレー')
      expect(response.body).to include('meal[food_names][]')
    end
  end
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/meals_spec.rb -e "frequently used"`
Expected: FAIL（最小フォームに quick ボタンが無い／`meal-food` controller 未配線）

- [ ] **Step 3: food_selector ヘルパーを実装**

In `app/helpers/application_helper.rb`, `regular_task_selector` の下に追記:

```ruby
  # 食べ物選択 UI（よく食べるもの + その他モーダル）。meal-food コントローラと連携。
  def food_selector(family)
    frequent = family.foods.frequently_used
    all_active = family.foods.active.ordered_by_name
    modal_id = "food_modal_#{SecureRandom.hex(4)}"

    content_tag(:div, class: 'space-y-2') do
      if frequent.any?
        concat(
          content_tag(:div, class: 'flex flex-wrap gap-2') do
            frequent.each do |food|
              concat(
                content_tag(:button, food.name, type: 'button',
                                                 class: 'btn btn-sm btn-outline btn-primary',
                                                 data: { action: 'meal-food#quickSelect', 'food-name': food.name })
              )
            end
            if all_active.size > frequent.size
              concat(
                content_tag(:button, 'その他...', type: 'button',
                                                  class: 'btn btn-sm btn-ghost',
                                                  data: { action: 'meal-food#openSelect' })
              )
            end
          end
        )
      end

      if all_active.any?
        concat(
          action_sheet_modal(id: modal_id, title: '食べ物を選択', controller: 'meal-food',
                             target: 'selectModal', close_action: 'closeSelect') do
            content_tag(:ul, class: 'menu w-full p-0') do
              safe_join(
                all_active.map do |food|
                  content_tag(:li) do
                    content_tag(:button, food.name, type: 'button',
                                                    class: 'py-4 px-6 w-full text-left active:bg-primary active:text-primary-content',
                                                    data: { action: 'meal-food#selectFromList', 'food-name': food.name })
                  end
                end
              )
            end
          end
        )
      end
    end
  end
```

- [ ] **Step 4: フォームを本格版に差し替え**

Replace `app/views/meals/_form.html.slim` の中身（食べ物セクションと、誰が select の追加）。`form_with` 〜 `.side-panel-footer` を以下に置換:

```slim
      .side-panel-body
        = form_with model: meal do |f|
          = hidden_field_tag :scope, scope
          .space-y-6
            - if meal.errors.any?
              .alert.alert-error
                ul
                  - meal.errors.full_messages.each do |msg|
                    li = msg

            .form-control
              = f.label :meal_type, '食事区分', class: 'label'
              = f.select :meal_type, Meal.meal_type_options, {}, class: 'select select-bordered w-full'

            .form-control
              = f.label :user_id, '誰が', class: 'label'
              = mobile_collection_select f, :user_id, current_user.family.users, :id, :login_id, { label: '誰が', include_blank: true, blank_value: '', blank_text: '家族みんな' }

            .form-control data-controller="meal-food"
              span.label-text 食べ物
              = food_selector(current_user.family)
              .flex.flex-wrap.gap-2.my-2 data-meal-food-target="chips"
                - meal.foods.each do |food|
                  span.badge.badge-lg.gap-1 data-food-name=food.name
                    input type="hidden" name="meal[food_names][]" value=food.name
                    span= food.name
                    button type="button" data-action="meal-food#removeFood" ✕
              .flex.items-center.gap-2
                input.input.input-bordered.flex-1 type="text" placeholder="新しい食べ物" data-meal-food-target="input"
                button.btn.btn-outline type="button" data-action="meal-food#addFromInput" 追加

            .form-control
              = f.label :date, '日付', class: 'label'
              = mobile_date_field f, :date

          .side-panel-footer
            = link_to 'キャンセル', '#', class: 'btn btn-ghost', data: { action: 'click->side-panel#close' }
            = f.submit meal.persisted? ? '更新' : '記録', class: 'btn btn-primary'
```

- [ ] **Step 5: テストが通ることを確認**

Run: `docker compose exec app bundle exec rspec spec/requests/meals/meals_spec.rb`
Expected: PASS

- [ ] **Step 6: RuboCop & コミット**

```bash
docker compose exec app bundle exec rubocop -A app/helpers/application_helper.rb
git add app/helpers/application_helper.rb app/views/meals/_form.html.slim spec/requests/meals/meals_spec.rb
git commit -m "feat: build meal form with food chip selector"
```

---

### Task 10: 日次ビューに食事セクションを統合

**Files:**
- Create: `app/views/calendar/_meal_section.html.slim`
- Create: `app/views/calendar/_meal_item.html.slim`
- Modify: `app/views/calendar/_daily_view.html.slim`
- Modify: `app/views/calendar/_family_daily_view.html.slim`
- Modify: `app/views/calendar/_my_daily_view.html.slim`
- Test: `spec/requests/calendar/calendar_spec.rb`（スコープ別の食事表示テストを新規追加）

**Interfaces:**
- Consumes: `@family_meals`（Task 4）、`new_meal_path` / `edit_meal_path` / `meal_path`（Task 5,7）、`side-panel-opener#openMealForm/openMealEdit`（Task 8）、`delete-confirm-trigger`（既存）。

- [ ] **Step 1: meal_item 部分テンプレートを作成**

Create `app/views/calendar/_meal_item.html.slim`:

```slim
.flex.items-center.justify-between.bg-green-50.border-l-4.border-green-400.p-2.rounded-r-lg
  .flex-1
    span.font-medium.text-gray-800= meal.foods.map(&:name).join('・')
    span.ml-2.text-xs.text-gray-500
      - if meal.user
        = meal.user.display_name
      - else
        | 家族
  .flex.space-x-1
    button.btn.btn-xs.btn-ghost[
      data-controller="side-panel-opener"
      data-action="click->side-panel-opener#openMealEdit"
      data-meal-id=meal.id
      data-scope=scope
    ]
      i.fas.fa-edit
    button.btn.btn-xs.btn-ghost.text-error[
      data-controller="delete-confirm-trigger"
      data-delete-confirm-trigger-url-value=meal_path(meal, scope: scope, date: meal.date)
      data-delete-confirm-trigger-message-value="この食事記録を削除しますか？"
      data-action="click->delete-confirm-trigger#open"
    ]
      i.fas.fa-trash
```

- [ ] **Step 2: meal_section 部分テンプレートを作成**

Create `app/views/calendar/_meal_section.html.slim`:

```slim
.mb-6.pb-4.border-b
  .flex.items-center.justify-between.mb-3
    h3.text-lg.font-bold.text-gray-800.flex.items-center
      span.mr-2 🍴
      | 今日何食べた？
    button.btn.btn-sm.btn-primary[
      data-controller="side-panel-opener"
      data-action="click->side-panel-opener#openMealForm"
      data-date=date.strftime("%Y-%m-%d")
      data-scope=scope
    ]
      i.fas.fa-plus.mr-1
      | 登録
  - if meals.any?
    .space-y-2
      - meals.group_by(&:meal_type).sort_by(&:first).each do |_meal_type, type_meals|
        .flex.gap-2
          .w-10.shrink-0.text-sm.font-semibold.text-gray-500.pt-2= type_meals.first.meal_type_text
          .flex-1.space-y-1
            - type_meals.each do |meal|
              = render 'calendar/meal_item', meal: meal, scope: scope
  - else
    .text-gray-400.text-sm 記録はありません
```

- [ ] **Step 3: _daily_view から family_meals を渡す**

Replace `app/views/calendar/_daily_view.html.slim`:

```slim
- if my_scope?
  = render "calendar/my_daily_view", date: date, family_plans: @family_plans, family_tasks: @family_tasks, family_meals: @family_meals, scope: current_scope
- else
  = render "calendar/family_daily_view", date: date, family_plans: @family_plans, family_tasks: @family_tasks, family_meals: @family_meals, target_users: @target_users, scope: current_scope
```

- [ ] **Step 4: family_daily_view のヘッダー直下に meal_section を挿入**

In `app/views/calendar/_family_daily_view.html.slim`, `= render "calendar/header", ...` の直後（`- target_users.each` の前）に追記:

```slim
  = render "calendar/meal_section", date: date, meals: family_meals, scope: scope
```

- [ ] **Step 5: my_daily_view のヘッダー直下に meal_section を挿入**

In `app/views/calendar/_my_daily_view.html.slim`, `= render "calendar/header", ...` の直後（`.mb-8.pb-4` の前）に追記:

```slim
  = render "calendar/meal_section", date: date, meals: family_meals, scope: scope
```

- [ ] **Step 6: スコープ別の食事表示テストを追加し green を確認**

`spec/requests/calendar/calendar_spec.rb` に describe を追加（既存の sign_in パターンに合わせる）:

```ruby
  describe 'meals in daily view' do
    let(:family) { create(:family) }
    let(:me) { create(:user, family: family) }
    let(:other) { create(:user, family: family) }

    before { sign_in me, scope: :user }

    it 'family scope shows all family meals for the date' do
      ramen = create(:food, family: family, name: 'ラーメン')
      meal = create(:meal, family: family, user: other, date: Time.zone.today, meal_type: 1)
      create(:meal_food, meal: meal, food: ramen)

      get calendar_path(date: Time.zone.today.to_s)
      expect(response.body).to include('ラーメン')
    end

    it 'my scope hides other members-only meals' do
      curry = create(:food, family: family, name: 'カレー')
      meal = create(:meal, family: family, user: other, date: Time.zone.today, meal_type: 2)
      create(:meal_food, meal: meal, food: curry)

      get my_calendar_path(date: Time.zone.today.to_s)
      expect(response.body).not_to include('カレー')
    end
  end
```

Run: `docker compose exec app bundle exec rspec spec/requests/calendar/calendar_spec.rb -e "meals in daily view"`
Expected: PASS（family は全件表示、my は他人専用を非表示）

- [ ] **Step 7: 全 spec & JS テスト & RuboCop**

Run: `docker compose exec app bundle exec rspec`
Expected: 全 PASS
Run: `yarn test:js`
Expected: 全 PASS
Run: `docker compose exec app bundle exec rubocop -A`
Expected: オフェンスなし

- [ ] **Step 8: コミット**

```bash
git add app/views/calendar/_meal_section.html.slim app/views/calendar/_meal_item.html.slim app/views/calendar/_daily_view.html.slim app/views/calendar/_family_daily_view.html.slim app/views/calendar/_my_daily_view.html.slim spec/requests/calendar/calendar_spec.rb
git commit -m "feat: integrate meal section into calendar daily views"
```

---

## Self-Review

**1. Spec coverage:**
- 3テーブル多対多（Food/Meal/MealFood）→ Task 1–3 ✓
- `active` フラグ・非表示（削除なし）→ Task 1（モデル）, Task 5（管理画面トグル）, Task 9（selector は active のみ）✓
- 「誰が」任意・null=家族 → Task 2（optional user, visible_to_user）, Task 7（user_id blank→nil）, Task 9（include_blank 家族みんな）✓
- 食事区分グルーピング表示 → Task 10（meal_section group_by meal_type）✓
- 日付ヘッダー直下・user_section の上 → Task 10 Step 4,5（header 直後に挿入）✓
- family=全件 / my=自分+null → Task 4（CalendarData）, Task 10（テスト green）✓
- side-panel 登録・食べ物チップ複数・find_or_create → Task 7,8,9 ✓
- よく食べるものサジェスト（meal_foods 件数）→ Task 1（frequently_used）, Task 9（food_selector）✓
- 設定画面に管理導線 → Task 6 ✓

**2. Placeholder scan:** 全ステップに実コード/実コマンドを記載。`_food_name_placeholder` は Task 7 の最小フォームの仮置きで、Task 9 で正式なチップUIに置換される（フォーム要素自体は完成形）。

**3. Type consistency:** `frequently_used(limit)`, `visible_to_user(user_id)`, `meal_type_options`, `meal_type_text`, `food_names`, `meal[food_names][]`, `openMealForm`/`openMealEdit`, `meal-food` targets（`chips`/`input`/`selectModal`）はタスク間で一貫。`food-name` data 属性 ↔ `dataset.foodName` 整合。

## 既知の仕様からの調整（実装時に注意）

- **`foods#index` の JSON 提供は見送り**（spec では言及）。既存 `regular_task_selector` 同様、selector はサーバサイド描画（`food_selector` ヘルパー）で完結するため JSON エンドポイントは不要（YAGNI）。将来 JS から動的取得が必要になった時に追加する。
- 「食べ物1件以上」は**モデルバリデーションではなくコントローラ**で担保（`Meal` 単体を素直に生成できるようにするため）。
- カレンダーセルへの食事インジケータは初版では出さない（セクション更新のみ）。
- Stimulus コントローラ登録は `app/javascript/controllers/index.js`（import + `application.register`）に追記する。
